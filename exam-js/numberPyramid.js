const showStars = (nbBase) => {
  for (let i = 0; i <= nbBase; ++i) {
    console.log(nbBase);
  }

  // for (let i = nbBase; i >= 1; --i) {
  //   console.log(nbBase.repeat(i));
  // }
};

showStars(8);
